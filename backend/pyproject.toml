[project]
name = "notesgram-backend"
version = "0.1.0"
description = "Notesgram backend (minimal)"
dependencies = [
  "fastapi>=0.95.0",
  "uvicorn[standard]>=0.22.0"
]

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
